[pytest]
testpaths = tests
addopts = -q -ra --strict-markers --strict-config
filterwarnings =
    error
markers =
    unit: fast unit-level tests.
    integration: cross-module integration tests.
    workflows: workflow orchestration behavior tests.
    scenarios: scenario-driven end-to-end behavior tests.
    persistence: data and repository persistence tests.
